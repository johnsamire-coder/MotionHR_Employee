include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - .dart_tool/**
    - .idea/**
    - android/**
    - ios/**

linter:
  rules:
    # avoid_print: false
    # prefer_single_quotes: true
